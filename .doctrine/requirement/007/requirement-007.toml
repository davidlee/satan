schema = "doctrine.requirement"
version = 1

id = 7
slug = "control-plane-operations-capability-decision-run-lifecycle-ledger-handoff-kill-are-requestresponse"
title = "Control-plane operations (capability decision, run lifecycle, ledger handoff, kill) are request/response; the data plane stays durable append-only logs"
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
