SATAN's percept includes recent git activity in `~/dev/satan`, commit subjects included. A live verification step that depends on the model's free choice can be steered by what the operator commits during the rollout.

**Observed (SL-016 PHASE-08, 2026-09-24).** VH-2 needed SATAN to ask a question in goad that the keeper would then leave unanswered. The attended run `20260924T204008-tick-pulse-3a5489` read the subject of commit `aaf84ba`. It judged the credit from the earlier VH-1 ask to be harness-derived, and it held its `self-work-check` motive quiet instead of asking (trace `20260924T204008-mbl66v`). The keeper waived VH-2, and the test VT-3 stood in.

**How to apply:** during a live rollout that waits on the model's own decision (an ask, a notification, a proposal), keep test-describing commits out of the tree until the observation is recorded. Otherwise, treat the model's choice as conditioned on them. A deterministic test over producer goldens is the stable evidence; a live VH is a watch.

Related: [[mem.fact.satan.intervention-classification-gate]], [[mem.fact.satan.green-is-not-green]].