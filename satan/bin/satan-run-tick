#!/usr/bin/env sh
# SATAN tick trigger.  Invokes `satan-tick' over emacsclient.
# The elisp side picks a mode from `satan-tick-pool' and applies
# quiet-hours suppression.  Returns the run-id (or nil on suppression).
set -eu
exec emacsclient --eval "(satan-tick)"
