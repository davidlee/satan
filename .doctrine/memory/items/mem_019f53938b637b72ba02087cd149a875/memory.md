# Corpus-integration tests skip-unless the ~/notes/satan corpus is present

Tests reading the host-only ~/notes/satan/ model-facing corpus must skip-unless the file is present (mirrors the DB skip-unless idiom)
