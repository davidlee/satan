id = 19
slug = "hermetic-test-db"
title = "Hermetic test DB with pinned satan-attrd schema"
status = "design"
created = "2026-09-23"
updated = "2026-09-23"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "flake.nix"
intent = "scope-relevant"

[[selector]]
selector = "flake.lock"
intent = "scope-relevant"

[[selector]]
selector = "justfile"
intent = "scope-relevant"

[[selector]]
selector = "dev/satan-test.el"
intent = "scope-relevant"

[[selector]]
selector = "satan/test/**"
intent = "scope-relevant"

[[relation]]
label = "fulfils"
target = "ISS-019"

[[relation]]
label = "fulfils"
target = "ISS-007"

[[relation]]
label = "governed_by"
target = "ADR-018"

[[relation]]
label = "related"
target = "ISS-013"

[[relation]]
label = "related"
target = "ISS-018"

[[relation]]
label = "related"
target = "RSK-015"

[[relation]]
label = "related"
target = "SL-017"

[[relation]]
label = "related"
target = "SPEC-001"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
