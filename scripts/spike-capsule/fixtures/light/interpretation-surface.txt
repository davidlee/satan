# Interpretation-surface declaration — LIGHT fixture (the `ledger` project).
#
# Default-deny; the dual of `.worktreeinclude` (DEC-099).
#
# AUTHORED FROM THIS PROJECT'S OWN BUILD NEEDS (PHASE-01 EX-12). The question
# answered here is only ever "what does `ledger` actually run to build, test,
# format and lint itself, and what does it read to do that?" — never "what can
# TypeScript or npm be made to evaluate?".
#
# That restraint is a CONTAMINATION GUARD, not a style note. PHASE-04 step 0
# enumerates the TypeScript/npm ecosystem's interpretation triggers WITHOUT
# consulting CPT-001, and its residue is the only thing in the whole rig that
# can falsify ASM-007's exhaustiveness claim. Enumerating hazards here would
# make step 0 a re-run of thinking already done, and an empty residue would
# then read as ASM-007 surviving when it had merely been echoed. Hazards that
# occurred to us while writing this are recorded in the PHASE-01 sheet's
# Findings, deliberately NOT here.
#
# This file is amended from step 0's residue afterwards (PHASE-04 EX-9).
#
# PROVENANCE (F-5): in the BASE fixture this lives OUTSIDE the repo, as a
# sibling of it. PHASE-05's variant (2) is the in-repo copy that manufactures
# the substitution exposure — QUE-201's only evidence input.
#
#   exec:       tokens the DQ-4 audit greps for; they must be ABSENT from
#               `control/**`. The control plane never runs the project's
#               toolchain.
#   interpret:  where a class-2/3 payload is planted for this fixture.
#   verify:     the one place project execution is wanted; runs inside the
#               verify capsule.

# STEP 0 AMENDMENT (PHASE-04 EX-9, 2026-08-01): NO CHANGE, and the reason is the
# finding. Recorded rather than left silent — same discipline as an `n/a` matrix
# cell, which is never dropped from the step list.
#
# Step 0's residue (R1-R4, `.doctrine/slice/241/step0-enumeration.md`) yields no
# new path for this fixture, because none of it is path-shaped:
#
#   R1  an LLM reading arbitrary tree content — the carrier is ANY file it
#       reads, which is exactly what an `interpret:` path list cannot express
#   R2  terminal escape sequences — the interpreter is the display channel
#   R3  untrusted metadata (ref names, commit messages) — not tree content
#   R4  non-inert parsing (prototype pollution) — the parsed files are
#       `package.json` and `tsconfig.json`, both ALREADY declared. It adds a
#       reason they matter, not a path.
#
# So the residue is precisely the part of the interpretation surface a
# path-based declaration is structurally unable to name. That is why ASM-007
# was invalidated rather than repaired, and it is tracked as QUE-203.
#
# The declaration below remains authored from this project's own build needs
# (PHASE-01 EX-12) and is unchanged.

exec:      node npm tsc
interpret: package.json tsconfig.json tools/format.mjs
verify:    npm test
