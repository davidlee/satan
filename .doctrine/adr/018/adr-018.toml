schema = "doctrine.adr"
version = 1

id = 18
slug = "daemon-topology-and-transport"
title = "Daemon topology and control-plane transport for the client end-state"
status = "proposed"
created = "2026-07-22"
updated = "2026-07-22"
tags = ["architecture", "boundary", "extraction"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-NNN <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-NNN related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "related"
target = "ADR-017"
