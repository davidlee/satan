#!/usr/bin/env sh
# SATAN run trigger.  Invokes Emacs's `satan-run' over emacsclient.
# Returns the run-id on stdout.
set -eu
mode="${1:?usage: satan-run MODE}"
exec emacsclient --eval "(satan-run \"$mode\")"
