schema = "doctrine.requirement"
version = 1

id = 11
slug = "every-invariant-holds-regardless-of-which-surface-initiates-the-action-surface-agnostic"
title = "Every invariant holds regardless of which surface initiates the action (surface-agnostic)"
status = "pending"
kind = "quality"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
