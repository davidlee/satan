schema = "doctrine.spec.product"
version = 1

id = {{id}}
slug = {{slug}}
title = {{title}}
status = "draft"
kind = "product"
tags = []
# parent = "PRD-NNN"   # decomposition parent (product→product)
