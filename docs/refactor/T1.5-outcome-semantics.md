---
name: satan-refactor-T1.5
description: Outcome-semantics design contract + negative classifier implementation
metadata:
  type: refactor-theme
  topic: satan-refactor
  status: merged
  blocked_by: [T1, T7]
  updated_at: 2026-05-23
---

# Theme T1.5 — Outcome-semantics design contract + negative classifier

**Impact:** High. **Effort:** L (design + implementation). **Risk:** M. **Reversibility:** Hard (lifecycle persisted in audit events).

`T1.5a` (design) blocks `T7` (audit shapes encode the vocabulary). `T1.5b` (implementation) is blocked by `T7` (needs substrate) and `T1` (extends `observer-classify.el`).

## Current shape

Today's positive classifier emits one verdict shape `(:verdict "positive" :predicate :Pn)` or `(:verdict "none" :reason :kw)`. Four positive predicates check for "intervention's motive saw worked-count increment". No confidence model; no maturity reasoning beyond a binary 30-minute `--mature-p` gate.

## Why it hurts

Attributes brief names five negative classes (`worked|neutral|ignored|contradicted|harmful|unknown`); each feeds Shame deltas in §3.3. Positive classifiers can be permissive because false positives mostly inflate confidence. Negative classification feeds Shame, so **false negatives and false positives both have behavioural consequences** — a noisy classifier produces a noisy Shame signal, biasing every downstream attribute Shame caps (Cruelty, Doubt).

Concretely:
- "Ignored" vs. "not yet attended to" is a temporal model problem, not a predicate problem.
- "Contradicted" needs the intervention's `expected_outcome` (a counterfactual) + evidence the user did the suspected behaviour + evidence an artifact emerged.
- "Harmful" needs causal reasoning that the codebase does not have.

Dropping these as new enum values into `observer-classify.el` understates the design.

## Target shape

Split into a *design contract* (T1.5a, no code) and an *implementation* (T1.5b, code).

T1.5a's deliverable is `docs/satan/attributes/outcome-semantics.md` defining: lifecycle states (`:pending|:mature|:stale`), candidate-verdict shape (`:classification :confidence :evidence :maturity :next-revisit-at`), required evidence handles per classification kind, confidence vocabulary (small/medium/high matching attributes.brief §3.3), manual marking semantics for `:harmful` (interactive command + `@satan-intervention-harmful` notes directive), and **an explicit list of what is NOT inferable in v1** (causal harm; contradiction without expected-counterfactual evidence; fine-grained ignored-vs-pending discrimination beyond the maturity window).

The contract must also pin down:
- **Clock + window semantics.** Which clock the maturity gate uses (recommend the broker's `:time_now` frozen at `--prepare`, mirroring A3 byte-identical-rerun). Late-evidence behaviour (does evidence arriving after `:stale` cutoff trigger a revision, or get dropped?). Whether `intervention.outcome_revised` is allowed at all.
- **Manual-marking workflow contract.** Who writes the manual `:harmful` event (interactive command + notes-side `@satan-intervention-harmful` directive both ultimately go through the same `intervention.outcome_classified` writer with `:source :manual`). Where the manual evidence lives (a freeform string + a pointer to the notes file + line that triggered the mark). How revisions of manual marks are audited (every revision emits a new `outcome_revised` event; the projection's `satan_intervention_outcomes` row reflects the latest).

T1.5b's implementation lands in `observer-classify.el` (already extracted by T1): verdict-shape extension, `classify-negative` returning `:ignored`/`:contradicted` only, `:harmful` rejected at API level (manual override only), `:neutral` and `:unknown` returned by the maturity coordinator.

## Migration sketch

- T1.5a (before T7): one PR, a design document. No code.
- T1.5b (after T7's substrate exists): one PR per step — verdict shape, negative predicates, lifecycle coordinator, human override path (interactive command + notes directive).

## Considered and rejected

- Single-enum verdict (just 4 more strings) — loses confidence + evidence + maturity; produces noisy Shame.
- Automatic `:harmful` in v1 — needs causal reasoning the codebase doesn't have; false-positive Shame is worse than missing-Shame.
- Skip `:unknown` / `:neutral` — removes the conservative-default escape hatch.

## First concrete step

Write `docs/satan/attributes/outcome-semantics.md` with the lifecycle + candidate-verdict + evidence-handle + non-inferables sections. Do *not* implement; the contract feeds T7's audit event schema.

## Open questions

- Confidence as continuous float or `:low|:medium|:high` enum? (Match attributes.brief: enum.)
- `:next-revisit-at` computed as `intervention.created_at + outcome_window_minutes`, with a 24h `:stale` cutoff?
- Where do user-supplied `:harmful` classifications enter the data flow?

## PR log

- [x] T1.5a — [`docs/satan/attributes/outcome-semantics.md`](../attributes/outcome-semantics.md) (doc only) — merged 2026-05-23. Resolves: confidence enum `:low|:medium|:high`; lifecycle states + 24h `:stale` cutoff; `outcome_window_minutes` declared at intervention create with per-kind defaults; `:harmful` / `:contradicted` manual-only in v1; revision policy (auto requires ≥ confidence while `:mature`; manual any time). Open questions punted to T7/T1.5b: id exposure to model; notes-directive vs auto ordering; counter-memory writer scope.
- [x] T1.5b PR 1 — verdict shape extension — merged 2026-05-23. `dl-satan-observer-classify` returns the §2 plist (`:classification :worked|:unknown`, `:confidence :low|:medium|:high`, `:predicates LIST`, `:reason KW`); runs all P1–P4 (not first-fire-wins) to derive `:high` on ≥2 firers; `:no_baseline` / `:crosses_midnight` / `:motive_dormant` / `:no_correlation` flow to `:unknown :low`. `dl-satan-observer--verdict-classify-args` consumes the new shape and emits the audit-event strings; `--persist-positive` trace metadata now carries `:predicates LIST` + `:classification` + `:confidence`. `dl-satan-observer-process` summary keys widened to match. PR 2 (negative predicates) will refine the no-fire branch into `:ignored` / `:neutral`.
- [x] T1.5b PR 2 — `classify-negative` predicates — merged 2026-05-23. `dl-satan-observer-classify-negative` refines the no-fire branch: user-facing kinds (`inbox`/`notify`/`visible_sign`/`proposal`/`patch_job`/`accuse`/`ask`/`surface`) classify `:ignored` (`:medium` when AFTER's panopticon focus probe is `ok` + zero post-emit segments; `:low` otherwise); non-user-facing kinds (`delay`/`quarantine` + anything else) classify `:neutral :low`; user-facing + ≥1 post-emit focus segment punts to `:unknown :low :reason nil` (per §1 — `:ignored` requires no acknowledgement event in window; new `:reason` keyword skipped to avoid contract amendment). `dl-satan-observer--assert-auto-classification` (`cl-check-type`) rejects auto verdicts with `:harmful` / `:contradicted`; guard wraps both `dl-satan-observer-classify` and `dl-satan-observer-classify-for-motives` returns. Writer `--verdict-classify-args` widens to emit snake-case `:ignored` / `:neutral` evidence (`:target_surface`, `:no_positive_predicates`, `:acknowledgement_checked`, `:ack_events_found`). Classifier-test fixture `--intervention` now carries `:kind "notify"` + `:target_surface "sway-mainbar"` by default; existing no-fire ert repointed and four new ert added (`:ignored` low/medium, `:neutral`, ack-events-found > 0 fall-through, persist `:ignored`/`:neutral`, auto-rejection of `:harmful`/`:contradicted`). Audit validator unchanged — `:ignored` / `:neutral` were already inside the closed classification set since T7.
- [x] T1.5b PR 3 — lifecycle coordinator — merged 2026-05-23. `dl-satan-observer--maturity-state intervention now` derives `:pending` / `:mature` / `:stale` from `(:ts, :outcome_window_minutes)` vs broker's frozen `:time_now` (§3 + §6.2; `:stale` cutoff = window-close + 24 h, matching `dl-satan-observer-scan-window-hours`). `dl-satan-observer-classify` gains `&optional NOW`: nil → maturity check skipped (test-fixture convenience, defaults `:maturity :mature`); `:pending` → emits `(:classification :unknown :confidence :low :reason :pending :maturity :pending)` without consulting motive / baseline / predicates (§2 invariant 3); `:stale` → returns nil so caller skips persist (§6.3 — auto re-pass forbidden); `:mature` → existing flow, `:maturity :mature` injected into every verdict. `dl-satan-observer-classify-for-motives` threads `NOW` through, with the same dispatch hoisted ahead of motive ranking. `dl-satan-intervention-pending` SQL extends with the 24 h stale exclusion (`i.ts + window*1min + 24h >= NOW`) so production never reaches the classifier's defensive `:stale` branch. `dl-satan-observer--verdict-classify-args` drops the hardcoded `:maturity "mature"` and reads from the verdict's `:maturity` slot (snake-cased at the audit boundary). `dl-satan-observer-process` passes `NOW` into `classify-for-motives`, captures nil verdicts as `(:skipped :stale)` in the summary, and now records `:maturity` per verdict for audit visibility. Audit validator unchanged — `:maturity` enum already accepts `pending|mature|stale` (T7) and invariant 3 (`pending ⇒ unknown`) was already enforced. 11 new ert: maturity-state at pending / mature-boundary / mature-inside-24h / stale; classify pending short-circuit; classify stale → nil; classify mature injects `:maturity :mature`; classify without `NOW` defaults to mature; classify-for-motives pending skips bundle; classify-for-motives stale → nil; pending SQL excludes stale rows (DB); persist `:pending` verdict writes `maturity="pending"` to projection (DB). PR 4 (manual override path) remains open.
- [x] T1.5b PR 4 — manual override path — merged 2026-05-23.  New `dl-satan-audit-reopen` returns an append-only audit handle for an existing run-dir (no truncate of `transcript.jsonl`, no manifest rewrite) so manual-mark writers attach `intervention.outcome_*` events to the iv's *original* run rather than a synthetic dir.  New `dl-satan-intervention-write-manual-outcome` is the single writer behind both surfaces; routes through `intervention-classify` with `:source "manual"` (auto-detects revision via prior outcome row), builds the §5 evidence plist (per-classification shape: `:harmful` carries `:reason` / `:evidence_pointer`; `:contradicted` carries `:prior_suspicion` / `:user_artifact`; both carry `:source_events ()` + `:marked_by`), then writes a §3.4 counter-memory trace via `dl-satan-memory-store-mark` with `:kind "observation"` / `:trace-origin "auto_rule"` / `:valence "negative"`, inheriting the intervention's `:cue_handles` so resonance surfaces the counter-memory on the same cue.  New `dl-satan-intervention-recent` query backs the interactive completing-read (newest-first, default-excludes-stale).  `dl-satan-intervention-mark.el` ships `my/satan-mark-intervention-{harmful,contradicted}` — completing-read over recent interventions (prefix-arg includes `:stale`), reads reason / evidence-pointer (defaults to buffer:line) / confidence (defaults `:medium`) / notes, resolves the iv's run-dir via `dl-satan-broker-locate-run-dir`, audit-reopens, dispatches.  Notes path widens `dl-satan-tools-atsatan.el`: `--rewrite-line` is now mark-aware (matches `@satan-intervention-{harmful|contradicted}` as a single prefix before the bare `@satan` fallback, so `@satan-intervention-harmful` cleans to `@satan-was-here` instead of corrupting into `@satan-was-here-intervention-harmful`); new `--parse-intervention-directive` parses the §7.2 grammar (`iv_id=… reason="…" [conf=low|medium|high] [evidence=path:line]`, conf defaults `low` per §4); new tool `notes_at_satan_intervention_done` reads the matched line, parses, locates the iv's run-dir, audit-reopens, calls the writer, then stamps the directive consumed with an `iv-<cls>: <comment>` tag.  Audit validator unchanged — manual `:harmful` / `:contradicted` were already accepted since T7.  18 new ert across `dl-satan-audit-test.el` (3 — reopen append + dir-missing + transcript-missing guards), `dl-satan-intervention-test.el` (5 — manual-writer rejects bad classification / marked-by, harmful first-emit with counter-memory cue handles, contradicted revises auto with §3.4 payload template), `dl-satan-intervention-mark-test.el` (5 — run-id parser, window-close derivation, recent newest-first / include-stale toggle, dispatch end-to-end with stubbed completing-read + read-string + broker locate + memory-store-mark), `dl-satan-tools-atsatan-test.el` (7 — parser happy / conf-default / missing-iv_id / bad-conf / wrong-prefix, rewrite-preserves-intervention-prefix, scanner-includes-and-rewrites end-to-end, done-refuses-without-capability).  Counter-memory trace handle inheritance ratified per outcome-semantics §11 open Q #3; audit-handle re-attach (§11 open Q #1 sub-question) confirmed safe with a dedicated `audit-reopen` that does not touch existing artifacts.
