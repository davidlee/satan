id = 16
slug = "goad-elicitation"
title = "goad as SATAN's elicitation surface"
status = "plan"
created = "2026-09-21"
updated = "2026-09-23"
tags = ["elicitation", "goad", "intervention", "perception", "satan"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "satan/satan-tools-*.el"
intent = "scope-relevant"

[[selector]]
selector = "satan/satan-percept.el"
intent = "scope-relevant"

[[selector]]
selector = "satan/satan-context.el"
intent = "scope-relevant"

[[selector]]
selector = "satan/satan-intervention.el"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/satan-memory-canon.el"
intent = "design-target"
note = "The design's own targets: the canon rule and evidence inclusion (DEC-004), both observer legs (DEC-012), and tick-pulse's capability plus quiet hours (DEC-006)."

[[selector]]
selector = "satan/satan-memory-evidence.el"
intent = "design-target"
note = "The design's own targets: the canon rule and evidence inclusion (DEC-004), both observer legs (DEC-012), and tick-pulse's capability plus quiet hours (DEC-006)."

[[selector]]
selector = "satan/satan-observer-classify.el"
intent = "design-target"
note = "The design's own targets: the canon rule and evidence inclusion (DEC-004), both observer legs (DEC-012), and tick-pulse's capability plus quiet hours (DEC-006)."

[[selector]]
selector = "satan/satan-tick.el"
intent = "design-target"
note = "The design's own targets: the canon rule and evidence inclusion (DEC-004), both observer legs (DEC-012), and tick-pulse's capability plus quiet hours (DEC-006)."

[[selector]]
selector = "satan/satan-tools.el"
intent = "scope-relevant"
note = "Tool registration and the capability token, the satan-goad-enabled kill switch, and the test tree — which lives at satan/test/, not a top-level test/."

[[selector]]
selector = "satan/satan-custom.el"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/satan-mode.el"
intent = "scope-relevant"
note = "Tool registration and the capability token, the satan-goad-enabled kill switch, and the test tree — which lives at satan/test/, not a top-level test/."

[[selector]]
selector = "satan/test/**"
intent = "scope-relevant"
note = "Tool registration and the capability token, the satan-goad-enabled kill switch, and the test tree — which lives at satan/test/, not a top-level test/."

[[selector]]
selector = "satan/satan-observer.el"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/satan-attribute.el"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/satan-audit.el"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/memory/migrations/0008_*.sql"
intent = "design-target"
note = "sec-9 design targets: queue rewrite + answer trace + evidence labels (observer); undelivered writer + :form on record/project/rebuild (intervention, DEC-024); suppression attributes; audit validator accepts :form; form_json migration; empty-XDG state root (DEC-027)."

[[selector]]
selector = "satan/satan-sensor-curiosity.el"
intent = "design-target"
note = "State-home readers moved onto the shared empty-XDG helper (DEC-027, RV-015 F-9)."

[[selector]]
selector = "satan/satan-tools-content.el"
intent = "design-target"
note = "State-home readers moved onto the shared empty-XDG helper (DEC-027, RV-015 F-9)."

[[relation]]
label = "governed_by"
target = "ADR-017"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "governed_by"
target = "POL-001"

[[relation]]
label = "related"
target = "RFC-016"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
