schema = "doctrine.knowledge"
version = 1

id = 2
slug = "op-read-in-live-session-never-prompts"
title = "A read inside a live 1Password session never prompts"
record_kind = "assumption"
status = "held"
created = "2026-09-23"
updated = "2026-09-23"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
claim = "With a live 1Password session (op whoami exits 0), op read of a ref returns silently and never raises the authorization dialog; only establishing a session prompts."
confidence = "medium"
basis = "observation"
validation_plan = "One clean test on the live host before PHASE execution relies on it: lock nothing, confirm op whoami exits 0, run op read on a cold ref (not in my/op--cache) and observe no dialog; repeat after the 1Password session-expiry window elapses to confirm whoami then fails."
validated_by    = ""
validated_on    = ""
invalidated_by  = ""
invalidated_on  = ""

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)

[[relation]]
label = "shapes"
target = "SL-018"
