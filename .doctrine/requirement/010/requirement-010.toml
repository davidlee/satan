schema = "doctrine.requirement"
version = 1

id = 10
slug = "with-the-client-absent-daemons-enforce-their-ceilings-and-never-fall-open-fail-closed"
title = "With the client absent, daemons enforce their ceilings and never fall open (fail closed)"
status = "pending"
kind = "quality"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
