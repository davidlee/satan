schema = "doctrine.requirement"
version = 1

id = 6
slug = "modetoolcapabilitybudget-policy-is-published-as-a-read-only-artifact-answerable-without-an-emacs"
title = "Mode/tool/capability/budget policy is published as a read-only artifact answerable without an Emacs image; surfaces read it, none restate it"
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
